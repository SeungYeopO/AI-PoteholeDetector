services:
  client:
    image: h2o_client
    container_name: client
    build:
      context: ./frontend
    ports:
      - "3000:3000"
  server:
    image: h2o_server
    container_name: server
    build:
      context: ./backend/poppy
    ports:
      - "8081:8081"